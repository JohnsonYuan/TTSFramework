<UserControl x:Uid="_ruler" x:Class="Microsoft.Tts.UI.Controls.Trajectory.DimensionRuler"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="_ruler">
    <Grid x:Uid="Grid_1">
    <Rectangle 
      x:Uid="_rulerArea"
      x:Name="_rulerArea" 
      Stroke="Transparent"
      Fill="Transparent"
      MouseEnter="OnRulerAreaMouseEnter"
      MouseLeave="OnRulerAreaMouseLeave"
      MouseMove="OnRulerAreaMouseMove"
      MouseLeftButtonDown="OnMouseLeftButtonDown"
      MouseLeftButtonUp="OnMouseLeftButtonUp"></Rectangle>
    <Canvas
      x:Uid="_selectionCanvas"
      x:Name="_selectionCanvas"></Canvas>
    <Canvas
      x:Uid="_currentSelectionCanvas"
      x:Name="_currentSelectionCanvas"></Canvas>
    <Popup 
      x:Uid="_labelPopup" 
      x:Name="_labelPopup" 
      PopupAnimation="Fade"
      HorizontalOffset="0" 
      Placement="Mouse">
      <Border 
        x:Uid="Border_1"
        BorderThickness="1"
        BorderBrush="Black">
        <Grid
          x:Uid="Grid_2"
          Background="White" >
          <TextBlock
            x:Uid="_popupText"
            x:Name="_popupText"
            Margin="2,0,2,0"/>
        </Grid>
      </Border>
    </Popup>
  </Grid>
</UserControl>
